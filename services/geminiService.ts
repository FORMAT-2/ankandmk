/**
 * API service removed as per user request.
 * Content is now managed statically in constants.tsx.
 */
export const generateRomanticMessage = async () => "";
export const generateScrambledWords = async () => [];
